name: Update Device Database

on:
  # Run monthly on the 1st at 00:00 UTC
  schedule:
    - cron: '0 0 1 * *'
  
  # Allow manual trigger
  workflow_dispatch:

jobs:
  update-database:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0
      
      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: 'gradle'
      
      - name: Grant execute permission for gradlew
        run: chmod +x gradlew
      
      - name: Generate device database
        run: ./gradlew generateDeviceDatabase
      
      - name: Check for database changes
        id: check_changes
        run: |
          # Check if the newly generated database differs from the committed one
          if git diff --quiet database/android-devices.db; then
            echo "has_changes=false" >> $GITHUB_OUTPUT
            echo "No changes detected in database"
          else
            echo "has_changes=true" >> $GITHUB_OUTPUT
            echo "Database changes detected"
            git diff --stat database/android-devices.db
          fi
      
      - name: Copy database to library assets
        if: steps.check_changes.outputs.has_changes == 'true'
        run: ./gradlew copyDatabaseToAssets
      
      - name: Get current date
        if: steps.check_changes.outputs.has_changes == 'true'
        id: date
        run: echo "date=$(date +'%Y%m%d')" >> $GITHUB_OUTPUT
      
      - name: Read base version
        if: steps.check_changes.outputs.has_changes == 'true'
        id: base_version
        run: |
          BASE_VERSION=$(grep -oP 'version = "\K[^"]+' build.gradle | head -1)
          # Remove any existing date suffix
          BASE_VERSION=$(echo "$BASE_VERSION" | sed 's/-[0-9]\{8\}$//')
          echo "version=$BASE_VERSION" >> $GITHUB_OUTPUT
          echo "Base version: $BASE_VERSION"
      
      - name: Update version in build.gradle
        if: steps.check_changes.outputs.has_changes == 'true'
        run: |
          NEW_VERSION="${{ steps.base_version.outputs.version }}-${{ steps.date.outputs.date }}"
          echo "New version: $NEW_VERSION"
          sed -i "s/version = \".*\"/version = \"$NEW_VERSION\"/" build.gradle
      
      - name: Read and increment database VERSION
        if: steps.check_changes.outputs.has_changes == 'true'
        id: db_version
        run: |
          CURRENT_VERSION=$(grep -oP 'private static final int VERSION = \K[0-9]+' library/src/main/java/com/jaredrummler/android/device/DeviceDatabase.java)
          NEW_DB_VERSION=$((CURRENT_VERSION + 1))
          echo "current=$CURRENT_VERSION" >> $GITHUB_OUTPUT
          echo "new=$NEW_DB_VERSION" >> $GITHUB_OUTPUT
          echo "Current DB VERSION: $CURRENT_VERSION"
          echo "New DB VERSION: $NEW_DB_VERSION"
      
      - name: Update VERSION in DeviceDatabase.java
        if: steps.check_changes.outputs.has_changes == 'true'
        run: |
          sed -i "s/private static final int VERSION = [0-9]\+;/private static final int VERSION = ${{ steps.db_version.outputs.new }};/" library/src/main/java/com/jaredrummler/android/device/DeviceDatabase.java
      
      - name: Update version in README.md
        if: steps.check_changes.outputs.has_changes == 'true'
        run: |
          NEW_VERSION="${{ steps.base_version.outputs.version }}-${{ steps.date.outputs.date }}"
          # Update the Maven dependency version in README
          sed -i "s/io.github.antonio2904:android-device-names:[^')]\+/io.github.antonio2904:android-device-names:$NEW_VERSION/" README.md
      
      - name: Commit changes
        if: steps.check_changes.outputs.has_changes == 'true'
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add build.gradle
          git add library/src/main/assets/android-devices.db
          git add library/src/main/java/com/jaredrummler/android/device/DeviceDatabase.java
          git add README.md
          git add database/
          git commit -m "chore: Update device database (${{ steps.date.outputs.date }})" -m "- Database VERSION: ${{ steps.db_version.outputs.current }} → ${{ steps.db_version.outputs.new }}" -m "- Library version: ${{ steps.base_version.outputs.version }}-${{ steps.date.outputs.date }}"
          git push
      
      - name: Import GPG key
        if: steps.check_changes.outputs.has_changes == 'true'
        run: |
          echo "${{ secrets.SIGNING_KEY }}" | base64 -d > /tmp/signing-key.gpg
          gpg --batch --import /tmp/signing-key.gpg
          rm /tmp/signing-key.gpg
      
      - name: Build and publish to Maven Central
        if: steps.check_changes.outputs.has_changes == 'true'
        env:
          ORG_GRADLE_PROJECT_sonatypeUsername: ${{ secrets.SONATYPE_USERNAME }}
          ORG_GRADLE_PROJECT_sonatypePassword: ${{ secrets.SONATYPE_PASSWORD }}
          SIGNING_KEY_ID: ${{ secrets.SIGNING_KEY_ID }}
          SIGNING_PASSWORD: ${{ secrets.SIGNING_PASSWORD }}
          SIGNING_SECRET_KEY_RING_FILE: /home/runner/.gnupg/secring.gpg
        run: |
          # Export GPG key to the location expected by Gradle
          gpg --batch --export-secret-keys --armor ${{ secrets.SIGNING_KEY_ID }} > /home/runner/.gnupg/secring.gpg
          
          # Publish to Maven Central
          ./gradlew publishToSonatype closeAndReleaseSonatypeStagingRepository --no-daemon --stacktrace
      
      - name: Create GitHub Release
        if: steps.check_changes.outputs.has_changes == 'true'
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: v${{ steps.base_version.outputs.version }}-${{ steps.date.outputs.date }}
          release_name: Device Database Update ${{ steps.date.outputs.date }}
          body: |
            ## Device Database Update
            
            Updated device database from Google's supported devices list.
            
            **Changes:**
            - Database VERSION: ${{ steps.db_version.outputs.current }} → ${{ steps.db_version.outputs.new }}
            - Library version: ${{ steps.base_version.outputs.version }}-${{ steps.date.outputs.date }}
            
            **Maven Dependency:**
            ```gradle
            implementation 'io.github.antonio2904:android-device-names:${{ steps.base_version.outputs.version }}-${{ steps.date.outputs.date }}'
            ```
          draft: false
          prerelease: false
